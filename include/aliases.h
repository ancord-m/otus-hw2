#ifndef ALIASES_H_
#define ALIASES_H_

#include <string>
#include <vector>

template<typename T>
using DoubleVector = std::vector<std::vector<T> >;

template<typename T>
using Vector = std::vector<T>;

#endif